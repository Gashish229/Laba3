#include <stdio.h>
#include <string.h>
int main(void)
{
    char name[50], familia[50];
    printf("Введите имя: ");
    scanf("%s", &name);
    printf("Введите фамилию: ");
    scanf("%s", &familia);
    printf("%s, %s\n", familia, name);
    getchar(); getchar();
    return 0;
}
